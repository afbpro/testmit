Options -MultiViews
RewriteEngine On
RewriteBase /api/

# Route all API endpoints to the modular router.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php/$1 [QSA,L]
